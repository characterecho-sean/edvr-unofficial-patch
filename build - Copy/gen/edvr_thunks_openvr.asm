; Generated by tools/gen_exports.py from openvr_api_orig.dll
; 14 named exports, 0 ordinal-only, 1 wrapped in C++.
; Do not edit: regenerate with build.bat.
;
; Each thunk is a single indirect jmp through a slot the DLL fills at load time
; with GetProcAddress against the real module. Leaf, no prologue, no unwind info
; needed, and the tail jump leaves the caller's frame and arguments untouched
; whatever the target's signature turns out to be.

.DATA

PUBLIC edvr_realProcs_openvr
edvr_realProcs_openvr QWORD 13 DUP(0)

.CODE

; Substituted for any export the real module does not provide: returning zero
; beats jumping through a null slot.
PUBLIC edvr_unresolved_openvr
edvr_unresolved_openvr PROC
    xor eax, eax
    ret
edvr_unresolved_openvr ENDP

.DATA

; Non-zero once the export table has been filled. Written by C, read by every
; thunk below. Byte-sized so the check is one compare against memory.
PUBLIC edvr_ready_openvr
edvr_ready_openvr BYTE 0

.CODE

EXTERN edvr_lazyInit_openvr:PROC

; Calls the C initialiser without disturbing anything the real function will
; expect to find.
;
; A thunk is transparent: it must not change the arguments, and on x64 the first
; four live in rcx/rdx/r8/r9 and xmm0-xmm3, all of which a call is free to
; clobber. So they are saved around it. Arguments five and up sit above the
; return address and are untouched, because rsp is restored exactly.
;
; Stack: a thunk entry has rsp = 8 (mod 16). The call into here makes it 0, and
; 128 is a multiple of 16, so rsp stays 0 (mod 16) -- correct for the call, and
; correct for the 16-byte movaps slots at rsp+64 and above. 32 bytes at the
; bottom are the shadow space the callee is entitled to.
; PROC FRAME, and the prologue annotated, so this function has unwind info.
;
; It did not, and that is not cosmetic. x64 exception handling walks the stack
; with the .pdata/.xdata tables; a function with no entry is assumed to be a leaf
; and its return address is read from [rsp]. This one moves rsp by 128 and then
; CALLS, so an unwinder would have taken the return address out of the middle of
; the shadow space below -- and every SEH handler above, including the game's,
; would fail to run. A fault in the initialiser became an uncatchable process
; kill rather than the degrade-to-vanilla this project promises.
;
; Confirmed by dumpbin: without these, thunks.obj contributes no .pdata at all.
edvr_lazyShim_openvr PROC FRAME
    sub     rsp, 128
    .ALLOCSTACK 128
    .ENDPROLOG
    mov     QWORD PTR [rsp+32], rcx
    mov     QWORD PTR [rsp+40], rdx
    mov     QWORD PTR [rsp+48], r8
    mov     QWORD PTR [rsp+56], r9
    movaps  XMMWORD PTR [rsp+64], xmm0
    movaps  XMMWORD PTR [rsp+80], xmm1
    movaps  XMMWORD PTR [rsp+96], xmm2
    movaps  XMMWORD PTR [rsp+112], xmm3
    call    edvr_lazyInit_openvr
    movaps  xmm3, XMMWORD PTR [rsp+112]
    movaps  xmm2, XMMWORD PTR [rsp+96]
    movaps  xmm1, XMMWORD PTR [rsp+80]
    movaps  xmm0, XMMWORD PTR [rsp+64]
    mov     r9,  QWORD PTR [rsp+56]
    mov     r8,  QWORD PTR [rsp+48]
    mov     rdx, QWORD PTR [rsp+40]
    mov     rcx, QWORD PTR [rsp+32]
    add     rsp, 128
    ret
edvr_lazyShim_openvr ENDP

PUBLIC edvr_openvr_thunk_0
edvr_openvr_thunk_0 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 0]
edvr_openvr_thunk_0 ENDP

PUBLIC edvr_openvr_thunk_1
edvr_openvr_thunk_1 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 8]
edvr_openvr_thunk_1 ENDP

PUBLIC edvr_openvr_thunk_2
edvr_openvr_thunk_2 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 16]
edvr_openvr_thunk_2 ENDP

PUBLIC edvr_openvr_thunk_3
edvr_openvr_thunk_3 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 24]
edvr_openvr_thunk_3 ENDP

PUBLIC edvr_openvr_thunk_4
edvr_openvr_thunk_4 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 32]
edvr_openvr_thunk_4 ENDP

PUBLIC edvr_openvr_thunk_5
edvr_openvr_thunk_5 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 40]
edvr_openvr_thunk_5 ENDP

PUBLIC edvr_openvr_thunk_6
edvr_openvr_thunk_6 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 48]
edvr_openvr_thunk_6 ENDP

PUBLIC edvr_openvr_thunk_7
edvr_openvr_thunk_7 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 56]
edvr_openvr_thunk_7 ENDP

PUBLIC edvr_openvr_thunk_8
edvr_openvr_thunk_8 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 64]
edvr_openvr_thunk_8 ENDP

PUBLIC edvr_openvr_thunk_9
edvr_openvr_thunk_9 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 72]
edvr_openvr_thunk_9 ENDP

PUBLIC edvr_openvr_thunk_10
edvr_openvr_thunk_10 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 80]
edvr_openvr_thunk_10 ENDP

PUBLIC edvr_openvr_thunk_11
edvr_openvr_thunk_11 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 88]
edvr_openvr_thunk_11 ENDP

PUBLIC edvr_openvr_thunk_12
edvr_openvr_thunk_12 PROC
    cmp BYTE PTR [edvr_ready_openvr], 0
    jne @F
    call edvr_lazyShim_openvr
@@:
    jmp QWORD PTR [edvr_realProcs_openvr + 96]
edvr_openvr_thunk_12 ENDP

END
