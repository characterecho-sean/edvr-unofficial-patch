# The eye mask as a load lever (2026-09-16)

Sean's question: can EDVR alter the eye mask the headset reports, to reduce
rendering and DLSS load. His steers the same day: default to the mask the
runtime provides, estimate one when it provides none, and let the user control
it live from the performance menu.

## Status

- **State (2026-09-16 evening): mechanism B, the field-of-view trim, BUILT as
  d54b8b7 and INSTALLED to Frontier (11:22), NOT FLOWN.**
  `fix.fov_trim_vertical`, `_outer`, `_nasal` (integer degrees 0..30, live,
  Performance page; the keys are in the live Frontier ini): the game is told a
  narrower projection AND a smaller recommended size at constant pixel density,
  so DLSS and every full-screen pass shrink with the rectangle; the treated
  image is placed into the matching sub-rect of the full-FOV swapchain, black
  beyond the trimmed edges. It rides the cull guard's stage machine; the
  d3d11/openvr_api frame ABI is v2 (a v1 half on either side runs with no
  trim). Journal: the "Mechanism B BUILT" entry has the design, the limits and
  the log formats.
- **The mask (A2) is FLOWN and priced (flight 2, 4449cf0, Frontier, Pimax):**
  both eyes drew every frame (8930 of 8930, scene depth targets #3 and #4, zero
  re-clears); trim 30 hides 87.6% of each viewport for about 0.55 ms of a 9.83
  ms GPU frame, trim 5 (25%) for 0.1-0.3 ms; no fps change at the 90 Hz cap
  (CPU waits 4-7 ms). RULED OUT as a frame-time lever: the depth-tested raster
  it reaches is under 0.6 ms, the rest is DLSS and
  full-screen passes priced by the rectangle. `fix.eye_mask` (off|auto|lens,
  `eye_mask_trim`) stays as p99 headroom, off by default. Eye order CONFIRMED
  by Sean: the trim-30 disc sat toward the nose. Flight 1 (c00879f) was null:
  eye keyed on foveation's attribution, dead under the port.
- **Per route** (table under Evidence): Pimax OpenXR and SteamVR/OpenXR with
  the Pimax driver supply NO mask for the Crystal Super (`triangles=0/0`); the
  Oculus runtime and SteamVR/OpenXR with the Oculus driver supply 52 triangles,
  27 per eye. With the guard or a trim staged the port withholds the mask on
  every route (`reason=modified_frustum`).
- **Elite reads the mesh once per session**, so a live mask control cannot go
  through GetHiddenAreaMesh: EDVR draws the mask itself (A2).
- **The mask cannot touch the DLSS cost.** NGX is priced by the output
  rectangle (0.18 ms + 0.097 ms per output megapixel per eye; 3.3-3.6 ms per
  stereo pair at 4068x4016). Only a narrower field of view told to the game (B)
  reduces it. Ruled out: multi-rectangle evaluations (two extra fixed costs eat
  what they could save, plus the foveation arc's seam class).
- **Z2 answered (09:18 flight, guard off):** Pimax's crop narrows the
  projection (up/down 51.7 to 41.4 deg, outer 56.8 to 54.1, inner 45.9 to 43.6)
  AND shrinks the targets (2576x1951 in, 3964x3001 out, 23% fewer pixels; GPU
  p50 7.6 ms against 9.0-9.8 uncropped), raising density about 10% per axis;
  the EDVR trim keeps density, so it saves 36% of the pixels for the same
  field. Z1: Pimax Play's Hidden Area Mask switch was already on, still 0/0. Z3
  (an estimated mask via foveation's ring) is moot.
- **Open, needs an instrument:** whether Elite draws the mesh it is given
  (Quest 3 routes); the runtime mask's area fraction; boundary artefacts from
  screen-space passes reading near depth along an EDVR-drawn edge.
- **Next: FLY the trim on Frontier.** Set Trim view, top and bottom to 10 on
  the Performance page (cull guard can stay off), then try outer and nasal.
  Read `python tools\edvr_log.py --target frontier --expect-build d54b8b7 --tag
  openxr --grep "native_cull,"`: expect a `stage=2` (adopting) line and then a
  `stage=3` (live) line within a second or two of the change, with
  `trim=0.0/0.0/10.0` (outer/nasal/vertical as applied), `factors=` below 1,
  `recommended=` smaller, and `placement=` narrowing from 0/0/1/1 to the
  sub-rect on the live line. In the gfx log `native benchmark:` and `native
  temporal: engaged ... output=` sizes must shrink by the same factor. Eyes:
  black only at the trimmed edges, no double image or swim at the border, menu
  and HUD in place; the GPU frame should move by more than the mask's 0.55 ms.
  Failure signatures: no `native_cull,` line = the trim never reached the
  runtime (key unread, or a pre-trim d3d11.dll answered v1 and zeroed it);
  `stage=2` never followed by 3 = the game did not rebuild in the direction
  asked; `stage=4` = inert, the trim arithmetic refused; stage 3 with unchanged
  gfx sizes = lie live, targets did not follow; a displaced or doubled picture
  = the placement or its jitter shift.

## Evidence

Logs read with `python tools\edvr_log.py --target <t> --tag openxr --nth
<n> --grep "visibility_mask,|hidden_mesh_query,|headset_key,"`.

| Route (headset_key) | runtime mask | to Elite | log |
|---|---|---|---|
| pimax-openxr/pimax-crystal-super | 0/0 | 0, empty | steam 2026-09-16 06:35, frontier 09-16 05:14 and every Pimax OpenXR log since 09-15 |
| steamvr-openxr/steamvr-openxr-aapvr (Pimax) | 0/0 | 0, modified_frustum | frontier 2026-09-15 06:31 |
| steamvr-openxr/steamvr-openxr-oculus (Quest 3, Steam Link) | 52/52 | 27 per eye, runtime | steam 2026-09-15 06:58 and 07:00 |
| oculus/meta-quest-3 (Air Link) | 52/52 | 0, modified_frustum | frontier 2026-09-15 08:34-08:40 |

The `modified_frustum` flights carry `native_cull,stage=2,factors=
1.19397/1.00000` lines; the first flight without the guard (frontier 2026-09-15
10:23) reads `empty` instead. openvr_system.cpp:339 returns the empty mesh when
`hiddenMasksCompatible` is false.

Code: the port requests XR_KHR_visibility_mask (native_runtime_host.h:1467 to
1483), reads the hidden triangle mesh (visibility_mask.h:22), and
`projectHiddenMask` (visibility_mask.h:56) reprojects the runtime's rays into
OpenVR's normalised space with the eye's raw FOV and a one-pixel inset guard
(native_runtime_host.h:274). `GetHiddenAreaMesh` (openvr_system.cpp:315) serves
a cached per-FOV mesh; the log line is throttled by GameCallProbeBudget
(call_probe_budget.h:17: four samples, then one per 20 s, sixteen in all). The
game's FOV and the submitted FOV come from one xrLocateViews result and diverge
only under the cull guard (native_runtime_host.h:763-766 rewrites gameGeometry,
:793 keeps the located views for submit).

Graphics side: ClearDepthStencilView is hooked, record only (vscreen.cpp:3410);
the depth probe learns each eye target's clear value and depth direction
(depth_probe.cpp:959, 976); eye attribution of render targets exists in
foveation.cpp (`eyeOf` :1229, `settleEyesByOrder` :1307) and is reached from
the Draw/DrawIndexed hooks through beginPanelOverride (vscreen.cpp:1484);
ui_depth.cpp has the OM/viewport save and restore helpers (:1409, :1433).
Nothing in the draw census recognises a hidden-area-mesh draw, so whether Elite
draws the mesh has never been observed. The performance page lists `[fix]` keys
whose `ui:` line carries `menu performance` (menu.cpp:1140).

Projection on the Crystal Super (workload line, radians): left eye
-0.99168/0.80133/0.90179/-0.90179, so tangents 1.529 outer, 1.032 inner, 1.265
up and down, spans 2.562 x 2.530 at 4068x4016 (16.3 MP).

## Mechanisms

**A1. Pass the runtime's mask through under the cull guard.** Today it is
withheld. The mask is a set of rays, the guard's frustum is known, so
projecting into the modified frustum instead of returning empty is a small
change in GetHiddenAreaMesh; whether `hiddenMasksCompatible` guards something
else (the submit crop) must be read before changing it. Worth only what Elite
does with the mesh, which is unobserved.

**A2. EDVR draws the mask itself.** After the game's clear of an eye-sized
depth buffer, draw the mask triangles depth-only at the near value the probe
learned for that target (0 for standard depth, 1 for reversed), colour writes
off, state saved and restored as ui_depth does. Every later draw into that
target fails the depth test in the region; G-buffer writes, the cost class the
cull freed, stop there. This is the only way a live control reaches the game,
it works on routes whose runtime supplies nothing, and it does not depend on
Elite honouring GetHiddenAreaMesh. Compute passes (lighting) and NGX are
unaffected. The mask shape: the runtime's mesh when supplied, else a disc about
the optical axis at the widest frustum edge (hides only what a circular lens
cannot show; 7.5% here), and the user's radius below that as the live trade.
Risk to look for: a halo along the boundary from screen-space passes reading
near depth; the guard inset does not cover it.

**B. Trim the field of view told to the game.** Report narrower tangents and a
size scaled by the span ratio (pixel density kept), and place the game's
texture into the matching sub-rectangle of the full-FOV swapchain in the draw
the port already makes, black outside. That keeps the layer's FOV honest, which
matters because the Oculus runtime ignores a narrower layer FOV. Every
per-pixel cost shrinks with the area, NGX included; the game's culler follows
the reported projection (the cull guard proved it). Live through the size
change, as the guard's staging is (a rebuild, a few seconds). Cost: black
beyond the trim. The same radius could drive A2's disc and B's rectangle as its
bounding box.

| trim (all edges) | area | NGX per pair | saving |
|---|---|---|---|
| none | 1.00 | 3.5 ms | |
| 3 deg | 0.81 | 2.9 | 0.6 ms |
| 5 deg | 0.70 | 2.6 | 0.95 |
| 8 deg | 0.57 | 2.15 | 1.4 |
| outer edge 56.8 to 45 deg only | 0.79 | 2.9 | 0.65 |
| disc bounding box, 45 deg radius | 0.62 | 2.3 | 1.2 |

Model figures on this projection; the frame should save more than the pass
because the game's per-pixel work shrinks too (the fovea flight went the other
way, so measure, do not assume).

**C. Ruled out:** any mask shape reducing NGX (above); the mesh getter as a
live channel (above); OpenXR Toolkit style layer-FOV narrowing as the transport
on Oculus.

## No-build flights, Steam install

- **Z1 Pimax Play Hidden Area Mask on** (Pimax's own switch, Quick Tip #16).
  Read `visibility_mask,` and `hidden_mesh_query,`. `triangles=N/N` with N
  above zero means the runtime's mask exists and Sean's "default to the
  provided one" is served on the Pimax by the code in place.
- **Z2 Pimax Play FOV crop** (1.43.1). Read the `native benchmark workload` FOV
  radians and `openxr_render_size`. Changed radians mean the runtime lever
  reaches the game and prices B on his rig by eye and by `native benchmark`
  p50/p95/p99, A/B/A, 75 s arms, no menu use inside an arm.
- **Z3 estimated mask prototype:** fix.foveation on, foveation_centre ahead,
  foveation_outer_rate cull, ring keys live (all d3d11-side, no gaze). Its
  price at the default ring is on record (5-6 points); the look of a black rim
  at a chosen radius is the question. Pimax Play's DFR must stay off or the
  feature stands down.

## Build plan, if Sean proceeds

1. Instrument first: the runtime mask's area fraction on the `visibility_mask`
   line; a census tag for a depth-only draw whose vertex count is three times
   the delivered triangle count, first draw after the eye clear (does Elite
   draw the mesh); the depth probe sampling one masked corner pixel after the
   game's own draw.
2. A2 as a `[fix]` key with a `ui:` line on the performance page: auto (runtime
   mesh, else the lens disc), off, and a live radius. Log the effective mask
   (source, triangles, area fraction, depth value used), never the key. Gate:
   native benchmark p50 and p99, and no boundary artefact when the radius is
   pushed inward on purpose.
3. A1 alongside, so cull-guard users keep the runtime mesh.
4. B as its own key, same page, after Z2 says what the trade looks like on the
   Pimax. Gate as the fovea arc's: at least 0.5 ms and 5% on the frame,
   geometry clean under head motion.

## Journal

- 2026-09-16: logs and code read, mechanisms priced, nothing built. ruled out:
  the mask as a DLSS lever, because NGX prices the output rectangle and the
  visible region's bounding box is the whole target. ruled out: live control
  through GetHiddenAreaMesh, because Elite reads it once per session (probe
  budget logs the first four calls; one per eye in every log).
- 2026-09-16: **BUILT, not flown.** fix.eye_mask = off | auto | lens plus
  fix.eye_mask_trim (-15..30, live), mechanism A2 exactly as scoped above: EDVR
  draws its own depth-only ring, never GetHiddenAreaMesh. New
  src/d3d11/eye_mask.h/.cpp.
  - **Hook:** vscreen.cpp's beginPanelOverride, right after
    depthProbeNoteEyeDraw, at each eye's first draw into its scene target
    this frame -- past foveation.cpp's own eyeOf/settleEyesByOrder via the
    new eyeOfSettled (foveation.h), independent of whether fix.foveation
    itself is armed.
  - **Past the hooks:** the draw and every state change around it go
    through vscreen.h's new *Raw wrappers (vScreenDrawRaw,
    VSSetShaderRaw, PSSetShaderRaw, VSSetConstantBuffersRaw,
    UpdateSubresourceRaw -- the same bypass vScreenSetRenderTargetsRaw
    already gave OMSetRenderTargets) for the five methods that ARE hooked,
    and straight through the context for IASetPrimitiveTopology,
    IASetInputLayout, GSSetShader, HSSetShader, DSSetShader, RSSetState,
    OMSetDepthStencilState and every getter, none of which are hooked at
    all. Draw census, eye-draw gate, foveation and the temporal pass never
    see it.
  - **Depth value:** never guessed -- depth_probe.h's
    depthProbeClearValueFor against the DSV OMGetRenderTargets returns at
    the draw (a referenced pointer, never the binding shadow's
    identity-only one); 1.0 if the recorded clear reads as reversed-Z, 0.0
    otherwise. depthProbeConfigure now watches whenever fix.eye_mask is on
    too, not only fix.temporal_aa.
  - **ABI:** common/frame_flag.h's announceRuntimeMaskTriangles /
    runtimeMaskTriangles, mapping version bumped v30 -> v31, gaze's
    presence-bit packing (0 triangles is a real answer here, unlike the
    zero-means-absent fields). native_runtime_host.h's refreshHiddenMasks
    now publishes the same triLeft/triRight it already logs. An older VR
    half that never calls the announce function reads back "not
    reported"; auto mode stands down rather than guess, and says so.
  - **Geometry:** centre (-(r+l)/(r-l), -(u+d)/(u-d)), R0 = widest of the
    eye's four frustum half-angles, R = clamp(R0-trim, 5, 89), semi-axes
    2 tan(R)/(r-l) and 2 tan(R)/(u-d), ring = triangle strip
    E_0,O_0..E_64,O_64 from SV_VertexID alone (no vertex buffer, no input
    layout), O_i = centre + (4/min(a,b))*(a cos, b sin) so the outer ring
    clears every corner regardless of how eccentric the frustum is. Pixel
    shader NULL, zero RTVs bound -- colour writes are structurally
    impossible, not merely unused.
  - **Centre sign, resolved:** the implementation brief's worked figure of
    -0.194 for the left eye's centre was a slip by the overseer, who quoted
    the projection matrix's m02 term directly. The optical axis sits at
    NDC x = -m02 = +0.194 (the outer, wider edge is on the left, so the
    frame's centre lies left of the axis), which is what the formula, the
    code and the smoke test all say. Nothing to change.
  - **Logs:** an effective-change line (mode; runtime triangles per eye or
    "not reported"; cone/edge/trim degrees; ellipse centre and semi-axes;
    masked percent; depth value written, the clear it came from and
    whether reversed-Z; whether this occurrence actually drew) fires per
    eye at startup, at a mode or trim change, and when that eye's own FOV
    tangents move more than 0.01; a 60 s cumulative summary (masked vs.
    observed frames per eye, re-clear count, stand-down tally) while on;
    "eye mask: waiting -- <reason>." once per distinct reason (eye not
    settled, no clear value on record for this depth target, no FOV yet,
    runtime mask not reported, shader or buffer setup failed); "eye mask:
    off." once, and again on any later transition back to off.
  - **Also new:** a per-eye "cleared again after the mask" census
    (eyeMaskOnClear, called from the same ClearDepthStencilView hook site
    depthProbeNoteClear already uses) feeding the 60 s summary, and
    tools/smoke's edvrEyeMaskSelftest (pure geometry, no device), gated
    into build.bat.
  - **Not flown.** No field evidence yet that either runtime route's
    depth test actually behaves the way this assumes; Z1/Z2/Z3 above are
    still open and are unrelated to this build.
- 2026-09-16: **Flight 1 (Frontier, build c00879f, log
  edvr_gfx_20260916_092725.log) drew nothing, root cause found and fixed.**
  Verbatim: `eye mask so far: eye 0 masked 0 of 0 frames, eye 1 masked 0 of 0
  frames; 0 frame(s) had the depth target cleared again after the mask;
  stand-downs: eye not settled=1052513, no clear value on record for this depth
  target=47352`. Cause: the hook identified its eye through `bindingResolve` +
  foveation.cpp's `eyeOfSettled`, which roots a target as an eye only when it
  equals `submittedTexture(e)` (src/common/frame_flag.cpp:430); under the
  OpenXR port that slot is written by src/d3d11/native_fss.cpp:118 with EDVR's
  own snapshot copy (`publishSubmitTexture(int(e), s->current[e].tex.Get())`,
  nulled at :113 once consumed), never the game's own texture -- the game's
  texture is published only by the retired proxy
  (src/openvr/compositor_hook.cpp:1552). Nothing ever rooted, so
  `settleEyesByOrder` (foveation.cpp:1307) had no member to pair from and
  `settled` stayed false for every target, every frame. Parity note for the
  foveation arc (recorded here only, not acted on): fix.foveation's own
  eyeOf/settleEyesByOrder has this identical blind spot under the native OpenXR
  port.
  - **Fix:** re-keyed entirely on the depth-stencil view, via the depth
    probe's own per-eye pick -- the identity the temporal pass already
    trusts. New `depthProbeSceneEyeOf(dsv, outEye, outTargetIndex)`
    (depth_probe.h/.cpp): `findTarget(dsv)`; false with outTargetIndex
    left at -1 if unknown; if the target is one of the two current
    `g_scenePick` entries, the eye is whichever of the pair was FIRST
    BOUND in the frame -- the same rule `depthProbeSceneDepth` uses,
    factored into one shared helper (`sceneOrderFirstSecond`) so the two
    can never disagree; otherwise false, with no order-based fallback for
    a same-sized target that simply is not (or is no longer) the pick.
    Sibling `depthProbeTargetIsSceneSized(index)` flags a not-picked
    target that shares the pick's own width/height (a double-buffered
    twin the game alternates with it). `eyeMaskOnEyeDraw` dropped its
    `rtv` parameter (vscreen.cpp's call site updated) and the whole
    `bindingResolve`/`eyeOfSettled` path; the once-per-eye-per-frame
    early-out now keys on the DSV identity (`g_maskedDsvThisFrame[2]`)
    instead of the RTV's. `eyeOfSettled` deleted from foveation.h/.cpp as
    dead code -- eye_mask.cpp was its only caller.
  - **Reason table:** "eye not settled" is gone; in check order in its
    place: "no depth target bound" (null DSV, before or after the real
    `OMGetRenderTargets` fetch), "depth target unknown to the probe"
    (`findTarget` failed) and "depth target not the scene's pick" (known
    to the probe, just not currently one of the two picks). "read-only
    depth view", "no clear value on record for this depth target", "no
    FOV yet", "runtime mask not reported" and "uav bound" are unchanged;
    "shader or buffer setup failed" (a different call site, unrelated to
    this hunt) stays too. The per-eye effective-change line gained `scene
    depth target #<index>`; the 60 s summary gained a per-period (not
    cumulative) count of distinct targets that stood down as "not the
    scene's pick" and how many of those were scene-sized, so a double-
    buffered pair the game alternates between shows in one line instead
    of costing a second flight.
  - **Overseer's addition, same day:** the probe's pick was only ever
    evaluated inside `depthProbeSceneDepth`, i.e. by whoever asked for the
    scene depth (the temporal pass, the interface depth, the two motion
    instruments), so on a rig with all of those off the mask would have
    stood down forever as "not the scene's pick". The evaluation is now one
    static helper (`refreshScenePick(w, h)`, hysteresis included) that
    `depthProbeSceneEyeOf` also runs, by the bound view's own size, whenever
    the view is outside the current pair; the hook reaches it only at an eye
    draw, where every bound view is eye-sized by D3D11's own rule, so it can
    never re-pick by a shadow map's size. The rim test also doubles as an
    eye-order check, because the ellipse centre sits on the nasal side
    (+0.194 for the left eye, so at trim 0 the ellipse touches the temporal
    edge and overshoots the nasal one): trimming in darkens the temple-side
    edge of each eye first; a rim on the nose side first means the
    first-bound target is the right eye, not the left.
  - **Flown the same day; see the next entry.**
- 2026-09-16: **Flight 2 (Frontier, build 4449cf0, log
  edvr_gfx_20260916_101816.log): the mask works; the price is small.** `eye
  mask so far: eye 0 masked 8930 of 8930 frames, eye 1 masked 8930 of 8930
  frames; 0 frame(s) had the depth target cleared again after the mask`; scene
  depth targets #3 (eye 0, centre +0.194) and #4 (eye 1, centre -0.194); every
  mode and trim Sean tried drew (`drew yes`): lens 30, 0, 10, 5, off, auto 5,
  auto 30, auto 5. The first minute stood down 3084 eye draws with no depth
  target bound and 1004 into 8 targets that were not the pick (4 of them
  scene-sized); none afterwards. GPU frame from the native benchmark windows
  (p50 / p95 / p99 ms): off, windows 10-11: 9.83 / 11.5-11.8 / 12.6-13.0; trim
  5 (25.4% hidden), windows 7-9 and 12-16: 9.5-9.75 / 10.3-11.6 / 11.6-15.7;
  trim 30 (87.6% hidden), windows 17-18: 9.2-9.3 / 10.8-10.9 / 11.8-12.4. CPU
  wait 4-7 ms per frame (`native timing CPU`): the game sits at the 90 Hz cap,
  so no overlay frame time moves whatever the GPU does.
  - **Ruled out: the lens mask as a frame-time lever**, because hiding 87.6%
    of every eye moved the GPU frame 0.55 ms. The depth-tested raster the
    mask reaches is under 0.6 ms of a 9.5 ms GPU frame; the roughly 3 ms of
    eye-draw work in performance.md is mostly not depth-culled (vertex- or
    draw-bound, or drawn with the depth test off), and the rest of the frame
    (DLSS at 3964x3914, lighting and post as full-screen passes) is priced by
    the rectangle, as the Status block said before the build.
  - Unread from the log: which way the trim-30 disc sat, the eye-order check.
    Sean to say: toward the nose in each eye = right; toward the temples =
    swapped.
  - The feature stays: safe (every stand-down reason fired only in the
    loading minute), live, and worth its p99 headroom at trim 5 for anyone at
    the cap. Default stays off in edvr.ini until Sean says otherwise.
- 2026-09-16: **Z2 answered from the 09:18 Frontier flight (build c00879f, log
  edvr_gfx_20260916_091855.log, openxr
  edvr_openxr_20260916_091857_039_26804.log, cull_stage=0 throughout): Pimax
  Play's FOV crop reaches the game as BOTH a narrower projection and a smaller
  render size.** `native benchmark workload` FOV radians L
  -0.94471/0.75996/0.72341/-0.72341 (tangents 1.380 outer, 0.951 inner, 0.883
  up and down; 54.1, 43.6 and 41.4 deg) against the full
  -0.99168/0.80133/0.90179/-0.90179 (1.529, 1.032, 1.265; 56.8, 45.9 and 51.7
  deg); `input 2576x1951` and `output 3964x3001` against 2576x2544 and
  3964x3914, so 23% fewer pixels in every pass; GPU p50/p95/p99 7.62/8.90/9.88
  ms over a completed 30 s window (2689 samples) against 9.0-9.8 ms p50 in the
  uncropped sessions the same morning (different scenes, so indicative, not an
  A/B). Sean's verdict: the crop gives a better result than the mask. It does,
  and the two are not the same lever: the crop shrinks the rectangle, so DLSS,
  lighting, post and the treatments all do 23% less; the mask keeps the
  rectangle and only stops depth-tested geometry shading inside the ring, which
  is why 88% hidden bought 0.55 ms. Accounting: the tangent spans narrowed 9%
  horizontally and 30% vertically while the width stayed 2576, so the crop also
  raised the pixel density about 10% per axis; an EDVR trim at constant density
  would save 36% of the pixels for the same field of view.
  - **Design notes for mechanism B, if Sean asks for it:** the port answers
    GetProjectionRaw and GetRecommendedRenderTargetSize, so it can narrow the
    projection and shrink the recommended size itself, live from the
    Performance page and per edge; trimming the nasal edges shrinks only the
    stereo overlap, not the total field of view, which Pimax's symmetric crop
    cannot do. To verify before building: whether Elite re-reads the
    recommended size only at a render-target rebuild (the rebuild logs
    suggest so; a live change would then land at the next graphics-setting
    toggle, not instantly). The submit must place the narrower texture
    correctly: Pimax OpenXR honours a narrower layer FOV (the crop flight is
    the runtime doing exactly that), the Oculus runtime does not (sub-rect of
    the full-FOV swapchain, as the Status block says). The cull guard and the
    trim both rewrite gameGeometry and must compose.
- 2026-09-16: **Mechanism B BUILT (not flown): a live field-of-view trim, per
  edge, at constant pixel density.** Three keys in `[fix]`, integer degrees,
  0..30, default 0, live on the Performance page: `fov_trim_vertical` (both the
  top and the bottom edge of each eye), `fov_trim_outer` (each eye's temple
  side) and `fov_trim_nasal` (each eye's nose side, which costs only the stereo
  overlap). The game is told the narrower projection AND the proportionally
  smaller recommended size, so every pass it runs is priced by the smaller
  rectangle -- including DLSS, whose output size follows the recommendation
  (`src\d3d11\native_temporal.cpp:198-199`) and whose NGX feature is re-created
  whenever that output size moves
  (`src\d3d11\dlaa.cpp:309-318`, the `f.outW != outW || f.outH != outH`
  test; the textures behind it likewise at `temporal_pass.cpp:3978-4009` and
  `4383-4398`). That is the whole difference from the mask, which keeps the
  rectangle.
  - **It is the cull guard's sibling, in the cull guard's own machine.**
    `NativeCullSettings` gained the three trims; the guard is active when a
    mode is set OR any trim is above zero, and a trim-only configuration
    runs the same Off -> WaitingScene -> Adopting -> Live stages, for the
    same reason the widening does: Elite rebuilds its render targets when
    the recommended size changes, and nothing may lie about the projection
    until it has. The new `target_` frustum is the trimmed true one, `lied_`
    is the widening applied to `target_` instead of `true_`, the crop at
    submit is target-inside-lied (exactly the whole image when no widening
    is asked for), and the size factors are now measured lied-against-TRUE,
    so they fall below 1. The channel to the graphics half cannot carry a
    factor below 1 (per-mille above 1.0, `frame_flag.h:499-509`), so it is
    told the widening alone -- target-against-lied, never below 1 -- and its
    stage semantics are unchanged.
  - **Edges and limits.** The outer edge is the horizontal tangent with the
    larger magnitude (eye 0's left, eye 1's right on every headset seen).
    An edge is trimmed as `t' = sign(t) * tan(atan(|t|) - deg)`. No edge is
    brought closer than 5 degrees to straight ahead and no axis is left
    under 20 degrees of span; when a pair of asks would breach the span,
    both are scaled back in proportion. What was actually applied is what
    the trace prints, so a clamp is visible rather than silent.
  - **Placement, and why the layer FOV stays located.** Pimax OpenXR honours
    a narrower layer FOV, the Oculus runtime does not (it takes the
    sub-rect of a full-FOV swapchain), so EDVR never narrows the layer: the
    treated image is drawn into the sub-rectangle of the swapchain image
    where the trimmed frustum sits inside the eye's own field
    (`placementBounds`, target-inside-true), the rest of the image is
    cleared black, and the layer keeps the located FOV that every runtime
    composes correctly. `D3D11Stereo::renderCaptured` gained a per-eye
    `StereoPlacement`; absent or full, it takes exactly the path it always
    took (full viewport, no clear). On an untreated frame the jitter tangent
    shift moves the rectangle instead of the layer FOV. The menu is drawn
    into the image, so it is published the content projection
    (`frameContentViews`), not the layer's.
  - **The d3d11/openvr_api ABI went to version 2**: `EdvrNativeFrameOutput`
    gained three floats at the end. The provider answers either version in
    the caller's own struct and writes nothing past it; a mismatched pair of
    DLLs therefore still runs, with no trim.
  - **What to read in a flight.** In the openxr log, `native_cull,` now
    carries its old fields first and then `trim=outer/nasal/vertical`
    (as applied), `target=` and `placement=` for eye 0. The line prints on
    each stage change, and the stage number is the enum's (Off 0,
    WaitingScene 1, Adopting 2, Live 3, Inert 4). A settings change with the
    scene not ready prints `stage=1` with zero trims; then expect `stage=2`
    and `stage=3` within a second or two of the scene, the placement identity
    on the first and the sub-rect on the second. In the gfx log the treated
    size is in `dlss: the feature is created ... in and WxH out` and in
    `temporal aa price: full-frame ngx, WxH` (the `native benchmark:` line's
    output is the XR target, which does not change); the NGX cost should
    fall with the output megapixels (0.097 ms per output megapixel per eye,
    from the cost probe). A flight has to show: the sizes shrink, the picture
    stays put with black only at the trimmed edges and no double image or
    swim at the border, the menu and the HUD land where they did, and the
    GPU frame moves by more than the mask's 0.55 ms.
  - Built and gated: `build.bat` green (config contract 261/261),
    `native_cull_test` 128 checks and `native_frame_test` 39 checks, both 0
    failures, plus the smoke rig. Committed as d54b8b7, rebuilt at that
    commit and INSTALLED to Frontier at 11:22 (`--verify-only` clean); the
    three keys with their `ui:` lines were added to the live Frontier ini
    after the cull guard block so they appear on the Performance page.
- 2026-09-16 (flight 3, 11:40-11:43, Frontier, Pimax, d54b8b7): **the trim
  WORKS and the DLSS saving is real; a change while live STALLED adoption.**
  Sean set vertical 10 and outer 10 with the scene not yet ready (two `stage=1`
  lines at 17:40:35 and 17:40:44 UTC: WaitingScene does print on a settings
  change, the brief was wrong), the scene arrived at 17:40:55 (`stage=2`,
  recommended 3964x3914 to 3248x2754, factors 0.819/0.704) and the lie went
  live 1.9 s later (`stage=3`, target -1.0656/1.0324/-0.8900/0.8900, placement
  0.1810/0.1482/1.0000/0.8518). The gfx log: the game's input went 2576x2544 to
  2111x1790, the DLSS feature was re-created at 2111x1790 in and 3248x2754 out
  (65% per axis, quality mode, preset K), and the NGX price fell from 3.38-3.40
  ms per stereo pair to 1.95 ms. The benchmark's GPU p50 did not move readably
  (5.0-5.6 ms before, 5.8-6.4 live, in changing scenes); Sean reports the frame
  time much better.
  - **The stall.** At 17:41:18 Sean set outer 5. The guard re-armed
    (`stage=2`, recommended 3566x2754, factors 0.899/0.704) and never went
    live: `reached` measured the rebuild against a baseline taken while the
    game still sat at the previous lie (2111x1790), with a factor relative
    to the runtime's size, so it waited for 0.899 x 2111 = 1898 px of width
    and the game built 2317x1790 (11:42:11, after a 1783x1377 excursion at
    50%). From 17:41:18 to the end of the flight at 17:43:28 the stage was
    Adopting: the game was told the smaller size but the TRUE projection, so
    it rendered the full field into a 3566x2754-sized target and the
    placement stayed identity, a full-field picture at 30% lower vertical
    density with no black edges, the DLSS output stretched 1.11 x 1.42 into
    the swapchain. Two of the flight's three minutes were spent there.
  - **Sean's "UI menu text swimming again".** Not localised by the log. The
    leading explanation is the stalled state above: text at 70% of the
    vertical density, stretched anisotropically by the placement blit, with
    the DLSS history restarting at each feature re-creation. Against the
    live state everything the log shows is consistent: `ui depth` engaged
    with 2 interface draws a frame and nothing left alone, the UI resolve
    ran, the temporal pass was given the lied frusta and the lied
    recommended size (`temporal.begin(gameGeometry, ...)`), treatments
    10/10 so the untreated-frame placement shift never applied.
    Discriminator for flight 4: whether the swim is there WITH the black
    edges visible (live) or only without them (stalled).
  - **Fix: adoption measures the rebuild against the ask the baseline was
    rendered for.** The guard records what the game is told at the end of
    every frame (`told_`); a fresh adoption's baseline pair is taken as
    rendered for that ask (`baselineAsk_`), and promotion wants
    `baseline x ask/baselineAsk` per axis, direction-aware as before. A
    change while a rebuild is in flight keeps the baseline and its ask, so
    the second ask promotes whether or not the game passed through the
    first. An ask that does not move an axis no longer waits for a size
    change on it. A grace of 270 frames (3 s) promotes a target that has
    moved at all when no ratio matches, because the game's own multiplier
    went 0.65 to 0.5 and back inside one adoption in this flight. New
    trace `native_cull_adopting,frames=,baseline=,for=,ask=,last=` as the
    baseline is seeded and every two seconds after, so a stall reads as
    numbers. native_cull_test gains the flight's sequence (live, outer 10 to
    5, the old target as baseline, 2318x1790 promotes), the change during
    adoption, the grace case and the never-moved case.
